name: prime-agent-demo
inherits: coding

runtime:
  backend: prime-agent
  config:
    prime_agent:
      persist_session: true
      session_dir: .superqode/prime-agent/sessions
      prompt_timeout: 900
      check_version: true

model_policy:
  primary: github-copilot/gpt-4.1
