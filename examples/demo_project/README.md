# Order summary demo

This project is intentionally incomplete. `summarize_orders()` must return the
number of orders, their total value rounded to two decimal places, and the
average order value rounded to two decimal places. An empty input must return
zeros for all three fields.

Run the tests with:

```bash
python -m unittest -v
```
